extends layout

block content
  h1= title
  p Welcome to #{title},
    if (typeof user !== 'undefined' && typeof user.facebookId !== 'undefined')
      span= user.facebookId
    else
      span friends
